node_modules/
# Generated, stateful, or non-deterministic outputs (regenerable via `npm run ci`).
# The committed sample REVIEW-REPORT.md + dashboard.html + reports/ show the format.
evidence-out/*.json
evidence-out/perf.json
!evidence-out/.gitkeep
