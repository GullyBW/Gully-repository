export const environment = {
  production: false,
  // Tirelo backend API. Point at your running payment/booking server.
  apiBaseUrl: 'http://localhost:4000/api',
  // Optional: set to enable the Google Maps JS SDK in the picker. When blank,
  // the picker uses backend geo (sandbox or live, depending on the server key).
  googleMapsApiKey: '',
};
